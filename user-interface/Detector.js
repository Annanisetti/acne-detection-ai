/*import React from 'react';
import { View, Text, Button } from 'react-native';

export default function Detector({ setShowDirectory }) {
  const handleHome = () => {
    setShowDirectory(false);
  };

  return (
    <View>
      <Text>Detector Screen</Text>
      <Button title="Home" onPress={handleHome} />
    </View>
  );
}
*/