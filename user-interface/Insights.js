/*import React from 'react';
import { View, Text, Button } from 'react-native';

export default function Insights({ setShowDirectory }) {
  const handleHome = () => {
    setShowDirectory(false);
  };

  return (
    <View>
      <Text>Insights Screen</Text>
      <Button title="Home" onPress={handleHome} />
    </View>
  );
}
*/